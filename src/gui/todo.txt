1) do/check all TODOs!
2) clean code, check delete;
4) improve API with sim obj;  50%/100%
5) check synch all constants from core to gui
6) defult path is from core to downlaoud window
7) clean all notudes images
8) check created directory (obj, build) in ../icp2024
9) makefile
10) doc
11) doxygen
x) ... =)



# MAKEFILE
ifeq ($(OS),Windows_NT)
    # Windows commands and settings
    RM = del /Q
    MKDIR = mkdir
    ZIP = zip
    QMAKE = qmake
    MAKE = mingw32-make
    RM_FLAGS = /F
    DIR_SEPARATOR = \\
else
    # Linux commands and settings
    RM = rm -f
    MKDIR = mkdir -p
    ZIP = zip -r
    QMAKE = qmake
    MAKE = make
    RM_FLAGS =
    DIR_SEPARATOR = /
endif

# Phony targets
.PHONY: all clean q_prepare run doxygen pack

all: q_prepare
        $(MAKE) -C src

q_prepare:
        $(QMAKE) -o src/Makefile icp2024.pro

run:
        ./bin/icp2024

clean: clean_pack
        $(RM) src/icp2024 obj/* moc/* bin/* src/qrc_icons.cpp src/ui_MainWindow.h src/Makefile src/.qmake.stash
        $(RM) -rf logs doc/html doc/latex

clean_pack:
        $(RM) xbatur00_xkukht01.zip

doxygen:
        doxygen doc/Doxyfile

pack: clean
        $(ZIP) xbatur00_xkukht01.zip src bin obj moc LICENSE Makefile README.txt icp2024.pro examples doc/Doxyfile

